## Packages
lucide-react | Standard icons used across the app
framer-motion | Page transitions and subtle entrance animations

## Notes
JWT token is stored in localStorage as 'token'.
API fetch calls must manually attach `Authorization: Bearer ${token}`.
